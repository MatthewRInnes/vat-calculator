/**
 * Aspect Ratio component for maintaining consistent width-to-height ratios
 * Useful for responsive images and media content
 */
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// Root component that maintains the specified aspect ratio
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
